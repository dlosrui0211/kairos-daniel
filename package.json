{
  
  "scripts": {
    "dev:win": "concurrently --kill-others \"sass -w scss:css --quiet-deps\" \"php -S localhost:80\" -n \"SASS,PHP\" -c \"bgBlue.bold,bgMagenta.bold\""
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "bootstrap-icons": "^1.11.3"
  }
}
